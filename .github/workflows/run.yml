name: Run project
on:
  pull_request:
    branches:
      - main
jobs:
  Run-App:
    runs-on: ubuntu-latest
    env:
      virtual_display_number: 99

    steps:
      - uses: TrackerSB/Green3Neo-Prepare-Action@main
      - uses: ./.github/actions/prepare-application-run
        with:
          virtual_display_number: ${{ env.virtual_display_number }}
      - name: Execute application
        run: just run
