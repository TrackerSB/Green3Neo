name: Build project
on:
  pull_request:
    branches:
      - main
jobs:
  Build-All:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        db-backend: ['PostgreSQL', 'MySQL']
    env:
      BUILD_DB_BACKEND: ${{ matrix.db-backend }}
    steps:
      - uses: TrackerSB/Green3Neo-Prepare-Action@main
      - uses: ./.github/actions/prepare-application-build
      - name: Execute full build
        run: just build
