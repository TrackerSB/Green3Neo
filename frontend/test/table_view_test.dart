import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets("Dummy", (tester) async {
    // This is a dummy test to ensure that the test suite runs without errors.
    // You can replace this with actual tests for your table view.
    expect(true, true);
  });
}
