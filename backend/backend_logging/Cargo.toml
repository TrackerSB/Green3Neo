[package]
name = "backend_logging"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["rlib"]

[dependencies]
backend_paths = { path = "../backend_paths" }
flexi_logger = { workspace = true }
log = { workspace = true }
